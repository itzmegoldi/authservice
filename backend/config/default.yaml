---
server:
  host: 0.0.0.0
  port: 8080
database:
  url: sqlite:///./data/auth.db
kafka:
  bootstrap_servers: localhost:9092
  integration_topic: auth.integration.verify
  worker_group: auth-service-workers
redis:
  host: localhost
  port: 6379
auth:
  issuer: http://localhost:8080
  token_ttl_minutes: 30
  bootstrap:
    admin_email: admin@example.com
    admin_password: ChangeMe123!
  keys:
    rsa_private_pem: ""
    rsa_public_pem: ""

